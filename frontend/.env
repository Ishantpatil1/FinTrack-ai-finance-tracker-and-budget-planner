VITE_API_BASE_URL=https://fintrack-ai-finance-tracker-and-budget-h9x4.onrender.com/api

# Local dev fallback (vite proxy still points to http://localhost:3000)
# For production builds, Vite will use this value. To override in Vercel set
# the environment variable `VITE_API_BASE_URL` to your backend URL.